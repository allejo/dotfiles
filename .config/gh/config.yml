aliases:
    prco: pr checkout
    prls: pr list
    prcr: pr create
    prv: pr view --web
    rc: repo clone
    rv: repo view --web
    pr-files: pr view --json files --jq '.files.[].path'
    reviews: pr list --search "user-review-requested:@me"
git_protocol: ssh
